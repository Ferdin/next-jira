//import Login from "@/components/Login";

export default function Home(): React.ReactElement {
  return (
    <div>
      {
        // First part is login page.
        // If the user is already logged in with checking the session. Then this page will redirected to dashboard. --> Dashboard Page
        // If the user is not logged in, then show login page.
      }
      {/* <Login /> */}
    </div>
  );
}
